
language: python
python:
    - 2.7
    - 3.6
install:
    - pip install pytest pytest-cov
    - pip install coveralls
    - pip install hypothesis
script:
    - py.test
after_success:
    - coveralls

