
# TODO: 1) Everything!

def components(partition):
	# A list of pairs (start, end) representing
	#  [start, end) if start < end, or
	#  [end, start) if start > end.
	# Each number must be covered exactly twice.
	
	pass
	return NotImplemented
	
	
	# Remove all the obvious curves. This reduces the number of places we have to look for new curves later.
	for index in lamination.triangulation.indices:
	
	to_split = ??
	while components:
		
		# Accelerate!!
		
		to_flip = ??
	
	return components

